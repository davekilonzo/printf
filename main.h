#ifndef MAIN_H
#define MAIN_H
#include <stdarg.h>
#include <stdlib.h>
#include <unistd.h>
int _putchar(char c);
int print_string(char *s);
int _printf(const char *format, ...);
int _strlen(char *s);

#endif
